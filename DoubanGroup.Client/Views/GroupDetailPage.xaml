<Page
    x:Class="DoubanGroup.Client.Views.GroupDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DoubanGroup.Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:designData="using:DoubanGroup.Client.DesignData"
    xmlns:mvvm="using:Prism.Windows.Mvvm"
    mc:Ignorable="d"
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    d:DataContext="{d:DesignInstance Type=designData:GroupDetailData, IsDesignTimeCreatable=True}">

    <GridView Background="#FFFFFF">
        <GridView.Header>
            <Grid Margin="40 20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Image Grid.RowSpan="2" Source="{Binding Group.LargeAvatar}" Height="100" Width="100" Margin="0 0 20 0"/>
                
                <TextBlock Grid.Column="1" Text="{Binding Group.Name}" VerticalAlignment="Center" Style="{StaticResource TitleTextBlockStyle}"/>
                
                <TextBlock Text="创建于2015年1月1日" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center"/>

                <Border Grid.Row="2" Margin="0 10 0 0" Background="#fff4e8" Grid.ColumnSpan="2" Padding="10">
                    <TextBlock Text="{Binding Group.Description}" TextWrapping="Wrap" MaxHeight="60">
                        <ToolTipService.ToolTip>
                            <TextBlock Text="{Binding Group.Description}"/>
                        </ToolTipService.ToolTip>
                    </TextBlock>
                </Border>
            </Grid>
        </GridView.Header>
    </GridView>
</Page>
